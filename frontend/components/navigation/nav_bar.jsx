import React from "react";

const NavBar = ({ logout }) => {
    return (
        <div className="nav-bar">
            <div>Search Bar</div>
            <div>Main Items</div>
            <div>Account Items</div>
        </div>
    );
};

export default NavBar;